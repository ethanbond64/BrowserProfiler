
export default class Engine {

    private heartbeat: number;
    private sleep: number;

    // constructor
    constructor(heartbeat: number, sleep: number) {
        this.heartbeat = heartbeat;
        this.sleep = sleep;
    }

    // start loop
    start() {
        // set awake
        // do loop
    }


    // stop loop
    stop() {
        // set asleep
    }

    // do loop
    private loop() {
        // do work
        // check if awake
        // if awake, do loop
    }